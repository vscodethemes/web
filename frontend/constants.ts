import { LanguageOptions, SortByOptions } from '@vscodethemes/types'

export const defaultSearchParams: any = {
  sortBy: SortByOptions.installs,
  page: 1,
  perPage: 12,
  lang: LanguageOptions.javascript,
}
